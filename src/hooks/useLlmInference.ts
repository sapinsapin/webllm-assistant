// Re-export from context for backward compatibility
export { 
  useLlmInference, 
  type ModelStatus, 
  type ChatMessage, 
  type BenchmarkResult,
  type EngineType,
  type EngineCapability,
} from "@/contexts/LlmInferenceContext";
